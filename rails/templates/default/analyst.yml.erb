{
  analysis_ip:  <%= @analyst[:analysis_ip].to_s %>,
  access_key_id: <%= @analyst[:access_key_id].to_s %>,
  clients: [ <%= @analyst[:clients].join(",") %> ],
  secret_access_key:  <%= @analyst[:secret_access_key].to_s %>
}
